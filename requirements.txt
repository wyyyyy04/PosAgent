# MenuPilot 核心依赖
# 安装: pip install -r requirements.txt

# Agent 框架 & LLM
langgraph>=1.2.0          # 工作流编排（StateGraph + checkpoint）
langchain>=1.3.0          # LangChain 集成（create_react_agent 等）
openai>=2.0.0             # LLM API 客户端（DeepSeek 兼容）

# Excel 读写
openpyxl>=3.1.0           # Excel 写入（保留原始格式）
pandas>=2.0.0             # 数据处理
numpy>=1.24.0             # 数值计算

# 匹配引擎
rapidfuzz>=3.0.0          # 快速模糊字符串匹配

# 可选：Embedding 兜底（默认关闭，不需要可跳过）
sentence-transformers>=2.0.0
faiss-cpu>=1.7.0
